# CursorRunner

CursorRunner - это приложение для macOS, которое создает эффект хаотичного движения курсора мыши. При запуске приложение захватывает скриншот экрана, отображает его в полноэкранном режиме и начинает перемещать курсор маленькими случайными шагами каждые 0.1 секунды, рисуя траекторию мягким синим цветом на изображении. Приложение выходит, когда пользователь двигает мышью.

## Особенности

- **Захват скриншота**: При запуске автоматически захватывает текущее изображение экрана.
- **Полноэкранный режим**: Отображает скриншот на весь экран.
- **Хаотичное движение**: Курсор перемещается случайными маленькими шагами (-10 до +10 пикселей по X и Y).
- **Рисование траектории**: Рисует путь курсора мягким синим цветом на скриншоте.
- **Выход по движению мыши**: Приложение завершается, как только пользователь двигает мышью.
- **Защита от отключения экрана**: Предотвращает автоматическое отключение экрана во время работы.

## Требования

- macOS (тестировано на Sonoma)
- Xcode или clang для сборки из исходников

## Установка

### Готовая сборка

1. Скачайте `run2.app` из релизов.
2. Запустите приложение двойным кликом.

Сборка из исходников и обход предупреждений macOS

1. Клонируйте репозиторий:
   ```
   git clone https://github.com/yourusername/CursorRunner.git
   cd CursorRunner
   ```
2. Соберите приложение с указанием минимальной версии macOS (например, 10.15):
   ```
   clang++ -mmacosx-version-min=10.15 -framework ApplicationServices -framework AppKit -framework CoreFoundation -framework IOKit main2.mm -o CursorRunner
   ```
3. Создайте .app пакет:
   ```
   mkdir -p CursorRunner.app/Contents/MacOS
   cp CursorRunner CursorRunner.app/Contents/MacOS/CursorRunner
   chmod +x CursorRunner.app/Contents/MacOS/CursorRunner
   ```

4. Создайте (или обновите) файл Info.plist в `CursorRunner.app/Contents/` с добавленными ключами разрешений:
   ```xml
   <key>NSScreenCaptureUsageDescription</key>
   <string>Приложению требуется доступ к захвату экрана для работы эффекта курсора.</string>
   <key>NSAccessibilityUsageDescription</key>
   <string>Приложению требуется доступ к управлению курсором мыши.</string>
   ```

5. Подпишите приложение (здесь показана ad-hoc подпись; для распространения используйте сертификат Apple Developer ID):
   ```
   codesign -s - CursorRunner.app
   ```

6. Для полного избегания предупреждений рекомендуется:
   - Подписывать приложение с действующим сертификатом Apple Developer ID.
   - Выполнить нотарификацию приложения через Apple.
 
 ## Использование
 
 1. Запустите `CursorRunner.app`.
2. Приложение захватит скриншот и начнет хаотично двигать курсор, рисуя траекторию.
3. Для выхода просто подвиньте мышь.

## Предупреждение

Это приложение может быть использовано как шутка или для демонстрации. Убедитесь, что оно не мешает важной работе.

## Лицензия

MIT License. См. LICENSE файл для деталей.

## Вклад

Pull requests приветствуются. Для крупных изменений, пожалуйста, сначала откройте issue для обсуждения.

## Контакт

Если есть вопросы, создайте issue в репозитории.
   codesign -s - run2.app
   chmod +x run2.app/Contents/MacOS/run
